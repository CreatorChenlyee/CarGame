import Scene from "../framework/Scene";

//@mark mdbg
const mdebug=window['wdebug'] && false
const {ccclass, property} = cc._decorator;

@ccclass
export default class {{ClassName}} extends Scene {

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {}

    //start () {}

    // update (dt) {}

    onInit(data:any){

    }
}
