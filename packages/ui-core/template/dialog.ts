import Dialog from "../framework/Dialog";

//@mark mdbg
const mdebug=window['wdebug'] && false
const {ccclass, property} = cc._decorator;

@ccclass
export default class {{ClassName}} extends Dialog {

    // LIFE-CYCLE CALLBACKS:

    // onLoad () {}

    //start () {}

    // update (dt) {}

    onInit(data:any){

    }
}
